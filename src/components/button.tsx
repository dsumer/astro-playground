import { styled } from "../theme/stitches.config";

export const Button = styled("button", {
  borderRadius: "9999px",
  fontSize: "13px",
  border: "0",
  bg: ["yellow", "red", "blue", "red", "blue"],
});
